import { ModeToggle } from './mode-toggle';

import type { Meta } from '@storybook/react';

const Story: Meta<typeof ModeToggle> = {
  component: ModeToggle,
  title: 'Inputs/ModeToggle',
};
export default Story;

export const Primary = {
  args: {},
};
